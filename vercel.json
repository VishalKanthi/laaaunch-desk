{
  "version": 2,
  "buildCommand": "npm run build && npm run bundle:server",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}
